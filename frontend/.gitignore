# Node.js dependencies
# This folder contains all the npm packages and should not be committed to version control.
node_modules/

# Build output
# Directories where build outputs or bundled files are generated. These should not be committed as they are generated from source code.
build/
dist/

# Environment files
# Environment variable files that might contain sensitive data (e.g., API keys) should be ignored.
.env

# Log files
# Log files created during the build process or runtime. They should be ignored to avoid committing unnecessary or potentially large log files.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
# Specific IDE/editor configuration files and directories. These are personal settings and should not be shared in version control.
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS-specific files
# System-specific files that are not needed in version control and are automatically generated by the operating system.
.DS_Store       # macOS specific file for Finder attributes
Thumbs.db        # Windows specific file for thumbnail cache

# Other files to ignore
# General patterns for ignoring various temporary and cache files.
*.log            # Log files of various types
*.cache          # Cache files that are auto-generated and can be regenerated
*.tmp            # Temporary files that are usually created during build or runtime processes
